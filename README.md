# Slides to Accompany MEAN Talk on/related to React-Pivot #

To view:

* Clone this repo
* Make sure mongo is running locally or modify `server/movie.js`
* `npm i[nstall]`
* `npm start`

All "slides" are in `client/`
